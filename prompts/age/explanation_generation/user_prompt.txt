Train-only label-conditioned statistics:

{SUMMARY_TRANSACTIONAL_STATS}

Factual demonstrations selected only from the training split (this block may be empty):

{FEW_SHOT_EXAMPLES}

Aggregated profile of the target client:

{CLIENT_STATS}

Choose the useful evidence and reasoning structure yourself. Use only the supplied data, provide a non-empty behavioral explanation, and finish with exactly one allowed `Final` label.
